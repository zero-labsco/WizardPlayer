// Placeholder for CocoaPods — actual native code is in the prebuilt static library.
import Flutter
import UIKit

public class LibtorrentFlutterPlugin: NSObject, FlutterPlugin {
  public static func register(with registrar: FlutterPluginRegistrar) {
    // No-op — all communication happens via Dart FFI, not platform channels.
  }
}
